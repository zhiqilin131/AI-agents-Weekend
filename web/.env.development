# Optional: call FastAPI directly (SSE). Comment out BOTH lines to use Vite proxy `/api` → :8765 with `npm run dev:all`.
# VITE_API_ORIGIN=http://127.0.0.1:8765

# WebGL slime (R3F + goo shader). Set to 0 to force SVG fallback.
VITE_SLIME_3D=1
