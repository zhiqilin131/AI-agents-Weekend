# Dev: call FastAPI directly so SSE streams are not buffered by Vite’s proxy.
# Start API + Vite: from this folder, `npm run dev:all` (see web/package.json).
# Must match API bind: `python -m uvicorn ... --host 127.0.0.1 --port 8765`
VITE_API_ORIGIN=http://127.0.0.1:8765
